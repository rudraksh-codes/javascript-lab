## CORE JAVASCRIPT FUNDAMENTALS

1. var(function scoped) let(block scoped) 
2. nested object destructing 
3. Optional chaining and null coalscing (to avoid Type error) 
4. spread (expand array/obj.) rest(collect)
5.template literal `${variable}` 
6."==" vs "===" , null==0 //false , undefined==0 //false, 
null==undefined //true
